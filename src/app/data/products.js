/**
 * Products data — placeholder for transition.
 * In production, products come from Laravel backend via Inertia props.
 * This file is retained ONLY for components that still import it during transition.
 * All page-level components should use props.products or props.equipment instead.
 *
 * @deprecated — Use Inertia props from the backend.
 */
export const products = [];
